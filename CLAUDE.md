# therapist_website

## Database schema changes

The app is pre-production. Do **not** generate or run migrations. Apply schema
changes with `npm run db:push` (drizzle-kit push) only. Migrations start once the
app is in prod.
